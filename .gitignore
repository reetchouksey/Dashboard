# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
build

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.idea
.vscode
*.local
*.swp
Thumbs.db

# env (but keep .env.example as a template)
.env
.env.*
!.env.example
.vite

# database files (regenerated from db.json on first run)
server/database.db
server/database.db-shm
server/database.db-wal

# DB export artefact (regenerate via `npm run db:view`)
database-data.json
